[project]
name = "ua-scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "bs4>=0.0.2",
    "hishel>=0.1.1",
    "httpx>=0.28.1",
    "loguru>=0.7.3",
    "pydantic>=2.10.6",
]

[project.scripts]
ua-scraper = "ua_scraper:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
